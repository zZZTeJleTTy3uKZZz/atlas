"""Замороженные vendored-копии бывших pseudo-китов с единственным потребителем.

Atlas #2710 (волна 5): ``agentskit`` и ``issuekit`` имели ровно одного
потребителя — Atlas — и по спеке платформы (2026-09-05-kits-platform-design.md,
таблица «Было → стало») переезжают в модуль Atlas. Публикация atlas как
библиотеки для реэкспорта нежелательна (CLI-продукт) — поэтому выбран
обратный путь: код физически скопирован сюда, зависимость на PyPI-пакеты
``s-agentskit``/``s-issuekit`` из ``atlas/pyproject.toml`` удалена, а исходные
киты остаются на диске в замороженном виде (не удалены — на случай второго
потребителя в будущем).

Это НЕ место для нового кода Atlas — только замороженные копии. Новую
функциональность добавляй в ``atlas.commands``/``atlas`` напрямую.
"""
from __future__ import annotations
